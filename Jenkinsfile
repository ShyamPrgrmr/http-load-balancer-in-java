pipeline {
    agent any

    stages {
        stage('Use API Token') {
            steps {
                 sh "ls -ltd"
		 sh '''
  			#!/bin/bash
			echo "$SHELL"

		    '''
            }
        }
    }
}
