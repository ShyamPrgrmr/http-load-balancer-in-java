pipeline {
    agent any

    stages {
        stage('build') {
            steps {
		 sh "cd /var/jenkins_home/workspace/"
                 sh "ls -ltd"
		 sh '''echo "$SHELL"'''

		
            }
        }
    
    }
}
